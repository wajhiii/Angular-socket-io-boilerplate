# Angular with node socket io boilerplate

Implementation of Angular socket client with node express socket.io.
 
### server side

`npm install`
`npm run start`

### client side

`cd client`
`npm install`
`ng serve`